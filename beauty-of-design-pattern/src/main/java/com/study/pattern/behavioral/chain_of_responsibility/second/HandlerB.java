package com.study.pattern.behavioral.chain_of_responsibility.second;

public class HandlerB implements IHandler {
    @Override
    public boolean handle() {
        boolean handled = false;
        // ...
        return handled;
    }
}
